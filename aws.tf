provider "aws-useast1" {
/*
    access_key = "${var.aws_access_key}"
    secret_key = "${var.aws_secret_key}"
*/
    region = "us-east-1"
}

provider "aws-useast2" {
/*
    access_key = "${var.aws_access_key}"
    secret_key = "${var.aws_secret_key}"
*/
    region = "us-east-2"
}